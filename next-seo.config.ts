const title = "Sklep testowy";
const description = "Sklep testowy oparty o Next.js 13";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    site_name: title,
    description,
  },
};

export default nextSeoConfig;
